<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sld.faq.module.faq.mapper.FaqItemMapper">

    <select id="searchPage" resultType="com.sld.faq.module.faq.entity.FaqItem">
        SELECT * FROM faq_item
        WHERE status = 1
        <if test="categoryId != null">AND category_id = #{categoryId}</if>
        <if test="keyword != null and keyword != ''">
            AND (question LIKE CONCAT('%', #{keyword}, '%') OR keywords LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        ORDER BY published_at DESC
    </select>

</mapper>
